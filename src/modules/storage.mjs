import BunnyHole from "./bunny_hole.mjs";

/* ************ *
 * STORAGE KEYS *
 ****************/
export const StorageKeys = Object.freeze({
    BUNNY_HOLE: "BunnyHole"
});

export const DefaultStorage = Object.freeze({
    [StorageKeys.BUNNY_HOLE]: undefined,
});